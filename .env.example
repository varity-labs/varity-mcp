# Varity MCP — environment variables. Copy to .env and fill in. Never commit actual values.

# GitHub personal access token with "repo" scope (used by the varity_create_repo tool)
GITHUB_TOKEN=

# Beta access token from Varity (used for MCP authentication)
VARITY_MCP_DEV_TOKEN=
